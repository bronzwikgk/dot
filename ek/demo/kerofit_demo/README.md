# kerofit_demo

This folder contains GitHub Pages-ready demos for Kerofit.

## Current demo

- `action3d-viewer/` - Interactive `<model-viewer>` page for converted `.glb` models.

## Publish path

If your Pages site is served from the `dot` repository root, this demo is available at:

`/ek/demo/kerofit_demo/action3d-viewer/`
