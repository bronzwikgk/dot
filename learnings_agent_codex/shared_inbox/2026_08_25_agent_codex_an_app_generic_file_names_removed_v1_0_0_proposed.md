# Shared Inbox: Generic File Names Removed

Date: 2026-08-25
Author: agent_codex_an_app
Status: added

## Summary

Generic active file names were removed from the current dot planning batch.

## Changes

- renamed active `README.md` files outside dependency folders to
  convention-style names
- renamed active uppercase proposal helper files to lower snake_case names
- changed new examples from generic `index` wording to `overview`
- left source-path references to old external `README.md` files as evidence,
  not active dot file names

## Tooling Note

Available coordination/design tooling found:

- `multi_agent_v1` for approved parallel agent work and handoffs
- Figma tools for visual architecture or design artifacts when requested
- InternalCore task tools for external task tracking when requested

No dedicated architecture/spec planning skill is currently installed, so dot
uses local docs, contracts, reports, and shared inbox notes as the default
planning system.
