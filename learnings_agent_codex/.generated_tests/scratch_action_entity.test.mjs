// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for D:\0dot1_Aug_2016_master\scratchpad_entity_system\code\action_entity.js
// regenerate instead of editing
import { test } from "node:test";
import * as assert from "node:assert";
import fs from "node:fs";
import path from "node:path";
import { pathToFileURL, fileURLToPath } from "node:url";
const mod = await import(pathToFileURL("D:/0dot1_Aug_2016_master/scratchpad_entity_system/code/action_entity.js").href);
const SNAPSHOT_DIR = fileURLToPath(new URL(".", import.meta.url));
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "scratch_action_entity.snapshots.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

async function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(await __call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

async function __construct(name, args) {
  const Ctor = mod[name] || (mod.default && mod.default[name]);
  if (typeof Ctor !== "function") return { threw: true, value: "__threw__:Error:no constructor target" };
  try { return { threw: false, value: __serialize(new Ctor(...args)) }; }
  catch (error) { const label = error && error.constructor ? error.constructor.name : String(error); return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") }; }
}
async function __call(name, args) {
  const fn = mod[name] || (mod.default && mod.default[name]);
  if (typeof fn !== "function") throw new Error("missing export " + name);
  return await fn.apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("memory_driver [constructor] construct", async () => {
  const __args = ["alpha"];
  const result = await __construct("memory_driver", __args);
  assert.strictEqual(result.threw, false, "constructor rejected generated arguments: " + result.value);
});

test("action_entity [constructor] construct", async () => {
  const __args = ["alpha", {}, "seed_value", {}];
  const result = await __construct("action_entity", __args);
  assert.strictEqual(result.threw, false, "constructor rejected generated arguments: " + result.value);
});

