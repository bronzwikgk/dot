- This repo contains wip code for testing.
- It should have a branch named tests, with folder for each project.
- For each project, there is a folder called tests containging a file with list of test to perform.
- It runs on ci/cd lifecycle via github and currently deployed at render.
- It has utils, actionGithub a nodejs utility to interact with github.
- A rule based actionAgent. That maintains this repo.
- Each branch should have an index of its folder and files, with last updated timestamp