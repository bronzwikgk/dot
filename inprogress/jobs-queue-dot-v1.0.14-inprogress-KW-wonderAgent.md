# Job Queue
version: v1.0.14
status: inprogress
agent: KW-wonderAgent

1. Build the native-only LocalStorage HTML tester requested by the user (no utilities, just raw browser APIs).
2. Make sure the button triggers multi-record writes/reads/updates/removals/clears while logging each step to the DOM and console.
3. Provide guidance for manual verification once the page loads, emphasising the I/O logging display.
