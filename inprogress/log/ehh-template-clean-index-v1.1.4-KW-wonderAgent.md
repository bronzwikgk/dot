project: ehh_template
version: v1.1.4
status: completed
agent: KW-wonderAgent
summary:
  - removed the bulky `node_index` checklist and the redundant `index:` properties from the workflow/directory/stats nodes in `shunya/index/ehh_template-v1.1.0-active-KW-wonderAgent.yaml`.
  - maintained the inline numbered headings (`1. project_manifest`, `1.1 overview`, etc.) so each child section now carries its own index without extra metadata.
