// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_array_slicing_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_array_slicing_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_array_slicing_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

const __ctor = mod;
let __instance = null;
try { __instance = new __ctor(); } catch (__e0) { try { __instance = new __ctor({}); } catch (__e1) {} }
function __call(name, args) {
  if (__instance === null) throw new Error("target unavailable: constructor rejected no-arg and object-arg instantiation");
  if (typeof __instance[name] !== "function") throw new Error("missing method " + name);
  return __instance[name].apply(__instance, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("execute [determinism] argset_0", () => {
  const __args = [[], "seed_value", "seed_value"];
  const first = __attempt("execute", __args);
  const second = __attempt("execute", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("execute [immutability] argset_0", () => {
  const __args = [[], "seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("execute", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("execute [snapshot] argset_0", () => {
  const __args = [[], "seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("execute", __args));
  const stored = __snapshots["execute"] && __snapshots["execute"][2];
  if (__capture || stored === undefined) {
    __snapshots["execute"] = __snapshots["execute"] || {};
    __snapshots["execute"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("execute [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const first = __attempt("execute", __args);
  const second = __attempt("execute", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("execute [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("execute", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("execute [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("execute", __args));
  const stored = __snapshots["execute"] && __snapshots["execute"][5];
  if (__capture || stored === undefined) {
    __snapshots["execute"] = __snapshots["execute"] || {};
    __snapshots["execute"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("execute [determinism] argset_2", () => {
  const __args = [[0], "seed_value", "seed_value"];
  const first = __attempt("execute", __args);
  const second = __attempt("execute", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("execute [immutability] argset_2", () => {
  const __args = [[0], "seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("execute", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("execute [snapshot] argset_2", () => {
  const __args = [[0], "seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("execute", __args));
  const stored = __snapshots["execute"] && __snapshots["execute"][8];
  if (__capture || stored === undefined) {
    __snapshots["execute"] = __snapshots["execute"] || {};
    __snapshots["execute"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("execute [determinism] argset_3", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const first = __attempt("execute", __args);
  const second = __attempt("execute", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("execute [immutability] argset_3", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("execute", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("execute [snapshot] argset_3", () => {
  const __args = [[3, 1, 2], "seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("execute", __args));
  const stored = __snapshots["execute"] && __snapshots["execute"][11];
  if (__capture || stored === undefined) {
    __snapshots["execute"] = __snapshots["execute"] || {};
    __snapshots["execute"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("execute [edge_safety] edge_0", () => {
  const __args = [[], null, null];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_1", () => {
  const __args = [[], "seed_value", "seed_value"];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_2", () => {
  const __args = [[null], "seed_value", "seed_value"];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], null, "seed_value"];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], undefined, "seed_value"];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "seed_value", null];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("execute [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "seed_value", undefined];
  try {
    __call("execute", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [determinism] argset_0", () => {
  const __args = [[], 0, 0];
  const first = __attempt("extractWindow", __args);
  const second = __attempt("extractWindow", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("extractWindow [immutability] argset_0", () => {
  const __args = [[], 0, 0];
  const before = JSON.stringify(__args);
  const result = __attempt("extractWindow", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("extractWindow [snapshot] argset_0", () => {
  const __args = [[], 0, 0];
  const actual = JSON.stringify(__attempt("extractWindow", __args));
  const stored = __snapshots["extractWindow"] && __snapshots["extractWindow"][2];
  if (__capture || stored === undefined) {
    __snapshots["extractWindow"] = __snapshots["extractWindow"] || {};
    __snapshots["extractWindow"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("extractWindow [determinism] argset_1", () => {
  const __args = [[3, 1, 2], -1, -1];
  const first = __attempt("extractWindow", __args);
  const second = __attempt("extractWindow", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("extractWindow [immutability] argset_1", () => {
  const __args = [[3, 1, 2], -1, -1];
  const before = JSON.stringify(__args);
  const result = __attempt("extractWindow", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("extractWindow [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], -1, -1];
  const actual = JSON.stringify(__attempt("extractWindow", __args));
  const stored = __snapshots["extractWindow"] && __snapshots["extractWindow"][5];
  if (__capture || stored === undefined) {
    __snapshots["extractWindow"] = __snapshots["extractWindow"] || {};
    __snapshots["extractWindow"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("extractWindow [determinism] argset_2", () => {
  const __args = [[0], 42, 42];
  const first = __attempt("extractWindow", __args);
  const second = __attempt("extractWindow", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("extractWindow [immutability] argset_2", () => {
  const __args = [[0], 42, 42];
  const before = JSON.stringify(__args);
  const result = __attempt("extractWindow", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("extractWindow [snapshot] argset_2", () => {
  const __args = [[0], 42, 42];
  const actual = JSON.stringify(__attempt("extractWindow", __args));
  const stored = __snapshots["extractWindow"] && __snapshots["extractWindow"][8];
  if (__capture || stored === undefined) {
    __snapshots["extractWindow"] = __snapshots["extractWindow"] || {};
    __snapshots["extractWindow"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("extractWindow [determinism] argset_3", () => {
  const __args = [[], 3.14, 3.14];
  const first = __attempt("extractWindow", __args);
  const second = __attempt("extractWindow", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("extractWindow [immutability] argset_3", () => {
  const __args = [[], 3.14, 3.14];
  const before = JSON.stringify(__args);
  const result = __attempt("extractWindow", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("extractWindow [snapshot] argset_3", () => {
  const __args = [[], 3.14, 3.14];
  const actual = JSON.stringify(__attempt("extractWindow", __args));
  const stored = __snapshots["extractWindow"] && __snapshots["extractWindow"][11];
  if (__capture || stored === undefined) {
    __snapshots["extractWindow"] = __snapshots["extractWindow"] || {};
    __snapshots["extractWindow"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("extractWindow [determinism] argset_4", () => {
  const __args = [[3, 1, 2], 42, 42];
  const first = __attempt("extractWindow", __args);
  const second = __attempt("extractWindow", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("extractWindow [immutability] argset_4", () => {
  const __args = [[3, 1, 2], 42, 42];
  const before = JSON.stringify(__args);
  const result = __attempt("extractWindow", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("extractWindow [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], 42, 42];
  const actual = JSON.stringify(__attempt("extractWindow", __args));
  const stored = __snapshots["extractWindow"] && __snapshots["extractWindow"][14];
  if (__capture || stored === undefined) {
    __snapshots["extractWindow"] = __snapshots["extractWindow"] || {};
    __snapshots["extractWindow"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("extractWindow [edge_safety] edge_0", () => {
  const __args = [[], 0, 0];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_1", () => {
  const __args = [[], 42, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_2", () => {
  const __args = [[null], 42, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], 0, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], -1, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], NaN, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], Infinity, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_7", () => {
  const __args = [[3, 1, 2], -Infinity, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_8", () => {
  const __args = [[3, 1, 2], 65536, 42];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_9", () => {
  const __args = [[3, 1, 2], 42, 0];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_10", () => {
  const __args = [[3, 1, 2], 42, -1];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_11", () => {
  const __args = [[3, 1, 2], 42, NaN];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_12", () => {
  const __args = [[3, 1, 2], 42, Infinity];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_13", () => {
  const __args = [[3, 1, 2], 42, -Infinity];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("extractWindow [edge_safety] edge_14", () => {
  const __args = [[3, 1, 2], 42, 65536];
  try {
    __call("extractWindow", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [determinism] argset_0", () => {
  const __args = [[], 0];
  const first = __attempt("slidingWindows", __args);
  const second = __attempt("slidingWindows", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("slidingWindows [immutability] argset_0", () => {
  const __args = [[], 0];
  const before = JSON.stringify(__args);
  const result = __attempt("slidingWindows", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("slidingWindows [snapshot] argset_0", () => {
  const __args = [[], 0];
  const actual = JSON.stringify(__attempt("slidingWindows", __args));
  const stored = __snapshots["slidingWindows"] && __snapshots["slidingWindows"][2];
  if (__capture || stored === undefined) {
    __snapshots["slidingWindows"] = __snapshots["slidingWindows"] || {};
    __snapshots["slidingWindows"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("slidingWindows [determinism] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const first = __attempt("slidingWindows", __args);
  const second = __attempt("slidingWindows", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("slidingWindows [immutability] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const before = JSON.stringify(__args);
  const result = __attempt("slidingWindows", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("slidingWindows [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const actual = JSON.stringify(__attempt("slidingWindows", __args));
  const stored = __snapshots["slidingWindows"] && __snapshots["slidingWindows"][5];
  if (__capture || stored === undefined) {
    __snapshots["slidingWindows"] = __snapshots["slidingWindows"] || {};
    __snapshots["slidingWindows"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("slidingWindows [determinism] argset_2", () => {
  const __args = [[0], 42];
  const first = __attempt("slidingWindows", __args);
  const second = __attempt("slidingWindows", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("slidingWindows [immutability] argset_2", () => {
  const __args = [[0], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("slidingWindows", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("slidingWindows [snapshot] argset_2", () => {
  const __args = [[0], 42];
  const actual = JSON.stringify(__attempt("slidingWindows", __args));
  const stored = __snapshots["slidingWindows"] && __snapshots["slidingWindows"][8];
  if (__capture || stored === undefined) {
    __snapshots["slidingWindows"] = __snapshots["slidingWindows"] || {};
    __snapshots["slidingWindows"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("slidingWindows [determinism] argset_3", () => {
  const __args = [[], 3.14];
  const first = __attempt("slidingWindows", __args);
  const second = __attempt("slidingWindows", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("slidingWindows [immutability] argset_3", () => {
  const __args = [[], 3.14];
  const before = JSON.stringify(__args);
  const result = __attempt("slidingWindows", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("slidingWindows [snapshot] argset_3", () => {
  const __args = [[], 3.14];
  const actual = JSON.stringify(__attempt("slidingWindows", __args));
  const stored = __snapshots["slidingWindows"] && __snapshots["slidingWindows"][11];
  if (__capture || stored === undefined) {
    __snapshots["slidingWindows"] = __snapshots["slidingWindows"] || {};
    __snapshots["slidingWindows"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("slidingWindows [determinism] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const first = __attempt("slidingWindows", __args);
  const second = __attempt("slidingWindows", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("slidingWindows [immutability] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("slidingWindows", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("slidingWindows [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const actual = JSON.stringify(__attempt("slidingWindows", __args));
  const stored = __snapshots["slidingWindows"] && __snapshots["slidingWindows"][14];
  if (__capture || stored === undefined) {
    __snapshots["slidingWindows"] = __snapshots["slidingWindows"] || {};
    __snapshots["slidingWindows"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("slidingWindows [edge_safety] edge_0", () => {
  const __args = [[], 0];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_1", () => {
  const __args = [[], 42];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_2", () => {
  const __args = [[null], 42];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], 0];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], -1];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], NaN];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], Infinity];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_7", () => {
  const __args = [[3, 1, 2], -Infinity];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("slidingWindows [edge_safety] edge_8", () => {
  const __args = [[3, 1, 2], 65536];
  try {
    __call("slidingWindows", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

