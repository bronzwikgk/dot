// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_validator_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\plugins\\code_shared_validator_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/plugins__code_shared_validator_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

const __ctor = mod;
let __instance = null;
try { __instance = new __ctor(); } catch (__e0) { try { __instance = new __ctor({}); } catch (__e1) {} }
function __call(name, args) {
  if (__instance === null) throw new Error("target unavailable: constructor rejected no-arg and object-arg instantiation");
  if (typeof __instance[name] !== "function") throw new Error("missing method " + name);
  return __instance[name].apply(__instance, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("validate [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validate", __args);
  const second = __attempt("validate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validate [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validate [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validate", __args));
  const stored = __snapshots["validate"] && __snapshots["validate"][2];
  if (__capture || stored === undefined) {
    __snapshots["validate"] = __snapshots["validate"] || {};
    __snapshots["validate"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("validate [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validate", __args);
  const second = __attempt("validate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validate [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validate [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validate", __args));
  const stored = __snapshots["validate"] && __snapshots["validate"][5];
  if (__capture || stored === undefined) {
    __snapshots["validate"] = __snapshots["validate"] || {};
    __snapshots["validate"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("validate [edge_safety] edge_0", () => {
  const __args = [null, null];
  try {
    __call("validate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate [edge_safety] edge_1", () => {
  const __args = [null, "seed_value"];
  try {
    __call("validate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value"];
  try {
    __call("validate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate [edge_safety] edge_3", () => {
  const __args = ["seed_value", null];
  try {
    __call("validate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined];
  try {
    __call("validate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateSchemaResolution [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateSchemaResolution", __args);
  const second = __attempt("validateSchemaResolution", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateSchemaResolution [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateSchemaResolution", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateSchemaResolution [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateSchemaResolution", __args));
  const stored = __snapshots["validateSchemaResolution"] && __snapshots["validateSchemaResolution"][2];
  if (__capture || stored === undefined) {
    __snapshots["validateSchemaResolution"] = __snapshots["validateSchemaResolution"] || {};
    __snapshots["validateSchemaResolution"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("validateSchemaResolution [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateSchemaResolution", __args);
  const second = __attempt("validateSchemaResolution", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateSchemaResolution [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateSchemaResolution", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateSchemaResolution [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateSchemaResolution", __args));
  const stored = __snapshots["validateSchemaResolution"] && __snapshots["validateSchemaResolution"][5];
  if (__capture || stored === undefined) {
    __snapshots["validateSchemaResolution"] = __snapshots["validateSchemaResolution"] || {};
    __snapshots["validateSchemaResolution"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("validateSchemaResolution [edge_safety] edge_0", () => {
  const __args = [null, null];
  try {
    __call("validateSchemaResolution", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateSchemaResolution [edge_safety] edge_1", () => {
  const __args = [null, "seed_value"];
  try {
    __call("validateSchemaResolution", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateSchemaResolution [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value"];
  try {
    __call("validateSchemaResolution", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateSchemaResolution [edge_safety] edge_3", () => {
  const __args = ["seed_value", null];
  try {
    __call("validateSchemaResolution", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateSchemaResolution [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined];
  try {
    __call("validateSchemaResolution", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateRuleFactEvaluation [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateRuleFactEvaluation", __args);
  const second = __attempt("validateRuleFactEvaluation", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateRuleFactEvaluation [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateRuleFactEvaluation", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateRuleFactEvaluation [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateRuleFactEvaluation", __args));
  const stored = __snapshots["validateRuleFactEvaluation"] && __snapshots["validateRuleFactEvaluation"][2];
  if (__capture || stored === undefined) {
    __snapshots["validateRuleFactEvaluation"] = __snapshots["validateRuleFactEvaluation"] || {};
    __snapshots["validateRuleFactEvaluation"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("validateRuleFactEvaluation [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateRuleFactEvaluation", __args);
  const second = __attempt("validateRuleFactEvaluation", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateRuleFactEvaluation [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateRuleFactEvaluation", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateRuleFactEvaluation [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateRuleFactEvaluation", __args));
  const stored = __snapshots["validateRuleFactEvaluation"] && __snapshots["validateRuleFactEvaluation"][5];
  if (__capture || stored === undefined) {
    __snapshots["validateRuleFactEvaluation"] = __snapshots["validateRuleFactEvaluation"] || {};
    __snapshots["validateRuleFactEvaluation"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("validateRuleFactEvaluation [edge_safety] edge_0", () => {
  const __args = [null, null];
  try {
    __call("validateRuleFactEvaluation", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateRuleFactEvaluation [edge_safety] edge_1", () => {
  const __args = [null, "seed_value"];
  try {
    __call("validateRuleFactEvaluation", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateRuleFactEvaluation [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value"];
  try {
    __call("validateRuleFactEvaluation", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateRuleFactEvaluation [edge_safety] edge_3", () => {
  const __args = ["seed_value", null];
  try {
    __call("validateRuleFactEvaluation", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateRuleFactEvaluation [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined];
  try {
    __call("validateRuleFactEvaluation", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateExecutionReadiness [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateExecutionReadiness", __args);
  const second = __attempt("validateExecutionReadiness", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateExecutionReadiness [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateExecutionReadiness", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateExecutionReadiness [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateExecutionReadiness", __args));
  const stored = __snapshots["validateExecutionReadiness"] && __snapshots["validateExecutionReadiness"][2];
  if (__capture || stored === undefined) {
    __snapshots["validateExecutionReadiness"] = __snapshots["validateExecutionReadiness"] || {};
    __snapshots["validateExecutionReadiness"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("validateExecutionReadiness [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("validateExecutionReadiness", __args);
  const second = __attempt("validateExecutionReadiness", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validateExecutionReadiness [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validateExecutionReadiness", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validateExecutionReadiness [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("validateExecutionReadiness", __args));
  const stored = __snapshots["validateExecutionReadiness"] && __snapshots["validateExecutionReadiness"][5];
  if (__capture || stored === undefined) {
    __snapshots["validateExecutionReadiness"] = __snapshots["validateExecutionReadiness"] || {};
    __snapshots["validateExecutionReadiness"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("validateExecutionReadiness [edge_safety] edge_0", () => {
  const __args = [null, null];
  try {
    __call("validateExecutionReadiness", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateExecutionReadiness [edge_safety] edge_1", () => {
  const __args = [null, "seed_value"];
  try {
    __call("validateExecutionReadiness", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateExecutionReadiness [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value"];
  try {
    __call("validateExecutionReadiness", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateExecutionReadiness [edge_safety] edge_3", () => {
  const __args = ["seed_value", null];
  try {
    __call("validateExecutionReadiness", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validateExecutionReadiness [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined];
  try {
    __call("validateExecutionReadiness", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("findSchemaForShape [determinism] argset_0", () => {
  const __args = ["seed_value"];
  const first = __attempt("findSchemaForShape", __args);
  const second = __attempt("findSchemaForShape", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("findSchemaForShape [immutability] argset_0", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("findSchemaForShape", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("findSchemaForShape [snapshot] argset_0", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("findSchemaForShape", __args));
  const stored = __snapshots["findSchemaForShape"] && __snapshots["findSchemaForShape"][2];
  if (__capture || stored === undefined) {
    __snapshots["findSchemaForShape"] = __snapshots["findSchemaForShape"] || {};
    __snapshots["findSchemaForShape"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("findSchemaForShape [determinism] argset_1", () => {
  const __args = ["seed_value"];
  const first = __attempt("findSchemaForShape", __args);
  const second = __attempt("findSchemaForShape", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("findSchemaForShape [immutability] argset_1", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("findSchemaForShape", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("findSchemaForShape [snapshot] argset_1", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("findSchemaForShape", __args));
  const stored = __snapshots["findSchemaForShape"] && __snapshots["findSchemaForShape"][5];
  if (__capture || stored === undefined) {
    __snapshots["findSchemaForShape"] = __snapshots["findSchemaForShape"] || {};
    __snapshots["findSchemaForShape"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("findSchemaForShape [edge_safety] edge_0", () => {
  const __args = [null];
  try {
    __call("findSchemaForShape", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("findSchemaForShape [edge_safety] edge_1", () => {
  const __args = [undefined];
  try {
    __call("findSchemaForShape", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [determinism] argset_0", () => {
  const __args = [[], "alpha"];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_0", () => {
  const __args = [[], "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_0", () => {
  const __args = [[], "alpha"];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][2];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("indexBy [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][5];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("indexBy [determinism] argset_2", () => {
  const __args = [[0], ""];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_2", () => {
  const __args = [[0], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_2", () => {
  const __args = [[0], ""];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][8];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("indexBy [determinism] argset_3", () => {
  const __args = [[], "  spaced  "];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_3", () => {
  const __args = [[], "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_3", () => {
  const __args = [[], "  spaced  "];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][11];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("indexBy [determinism] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][14];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("indexBy [determinism] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const first = __attempt("indexBy", __args);
  const second = __attempt("indexBy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("indexBy [immutability] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("indexBy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("indexBy [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const actual = JSON.stringify(__attempt("indexBy", __args));
  const stored = __snapshots["indexBy"] && __snapshots["indexBy"][17];
  if (__capture || stored === undefined) {
    __snapshots["indexBy"] = __snapshots["indexBy"] || {};
    __snapshots["indexBy"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("indexBy [edge_safety] edge_0", () => {
  const __args = [[], ""];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_1", () => {
  const __args = [[null], ""];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], ""];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], " "];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], "\t"];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "0"];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("indexBy [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "null"];
  try {
    __call("indexBy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

