// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_metric_calculation_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_metric_calculation_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_metric_calculation_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

const __ctor = mod;
let __instance = null;
try { __instance = new __ctor(); } catch (__e0) { try { __instance = new __ctor({}); } catch (__e1) {} }
function __call(name, args) {
  if (__instance === null) throw new Error("target unavailable: constructor rejected no-arg and object-arg instantiation");
  if (typeof __instance[name] !== "function") throw new Error("missing method " + name);
  return __instance[name].apply(__instance, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("calculateAccuracy [determinism] argset_0", () => {
  const __args = [[], []];
  const first = __attempt("calculateAccuracy", __args);
  const second = __attempt("calculateAccuracy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateAccuracy [immutability] argset_0", () => {
  const __args = [[], []];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateAccuracy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateAccuracy [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateAccuracy", __args);
  const second = __attempt("calculateAccuracy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateAccuracy [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateAccuracy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateAccuracy [determinism] argset_2", () => {
  const __args = [[0], [0]];
  const first = __attempt("calculateAccuracy", __args);
  const second = __attempt("calculateAccuracy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateAccuracy [immutability] argset_2", () => {
  const __args = [[0], [0]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateAccuracy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateAccuracy [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateAccuracy", __args);
  const second = __attempt("calculateAccuracy", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateAccuracy [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateAccuracy", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateAccuracy [edge_safety] edge_0", () => {
  const __args = [[], []];
  try {
    __call("calculateAccuracy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateAccuracy [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2]];
  try {
    __call("calculateAccuracy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateAccuracy [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2]];
  try {
    __call("calculateAccuracy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateAccuracy [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], []];
  try {
    __call("calculateAccuracy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateAccuracy [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null]];
  try {
    __call("calculateAccuracy", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateSharpeRatio [determinism] argset_0", () => {
  const __args = [[], "seed_value"];
  const first = __attempt("calculateSharpeRatio", __args);
  const second = __attempt("calculateSharpeRatio", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateSharpeRatio [immutability] argset_0", () => {
  const __args = [[], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateSharpeRatio", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateSharpeRatio [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "seed_value"];
  const first = __attempt("calculateSharpeRatio", __args);
  const second = __attempt("calculateSharpeRatio", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateSharpeRatio [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateSharpeRatio", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateSharpeRatio [determinism] argset_2", () => {
  const __args = [[0], "seed_value"];
  const first = __attempt("calculateSharpeRatio", __args);
  const second = __attempt("calculateSharpeRatio", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateSharpeRatio [immutability] argset_2", () => {
  const __args = [[0], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateSharpeRatio", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateSharpeRatio [determinism] argset_3", () => {
  const __args = [[3, 1, 2], "seed_value"];
  const first = __attempt("calculateSharpeRatio", __args);
  const second = __attempt("calculateSharpeRatio", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateSharpeRatio [immutability] argset_3", () => {
  const __args = [[3, 1, 2], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateSharpeRatio", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateSharpeRatio [edge_safety] edge_0", () => {
  const __args = [[], null];
  try {
    __call("calculateSharpeRatio", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateSharpeRatio [edge_safety] edge_1", () => {
  const __args = [[], "seed_value"];
  try {
    __call("calculateSharpeRatio", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateSharpeRatio [edge_safety] edge_2", () => {
  const __args = [[null], "seed_value"];
  try {
    __call("calculateSharpeRatio", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateSharpeRatio [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], null];
  try {
    __call("calculateSharpeRatio", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateSharpeRatio [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], undefined];
  try {
    __call("calculateSharpeRatio", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [determinism] argset_0", () => {
  const __args = [[], [], "seed_value"];
  const first = __attempt("calculateHitRate", __args);
  const second = __attempt("calculateHitRate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateHitRate [immutability] argset_0", () => {
  const __args = [[], [], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateHitRate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateHitRate [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2], "seed_value"];
  const first = __attempt("calculateHitRate", __args);
  const second = __attempt("calculateHitRate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateHitRate [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateHitRate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateHitRate [determinism] argset_2", () => {
  const __args = [[0], [0], "seed_value"];
  const first = __attempt("calculateHitRate", __args);
  const second = __attempt("calculateHitRate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateHitRate [immutability] argset_2", () => {
  const __args = [[0], [0], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateHitRate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateHitRate [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2], "seed_value"];
  const first = __attempt("calculateHitRate", __args);
  const second = __attempt("calculateHitRate", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateHitRate [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2], "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateHitRate", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateHitRate [edge_safety] edge_0", () => {
  const __args = [[], [], null];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2], "seed_value"];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2], "seed_value"];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], [], "seed_value"];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null], "seed_value"];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], [3, 1, 2], null];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateHitRate [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], [3, 1, 2], undefined];
  try {
    __call("calculateHitRate", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanAbsoluteError [determinism] argset_0", () => {
  const __args = [[], []];
  const first = __attempt("calculateMeanAbsoluteError", __args);
  const second = __attempt("calculateMeanAbsoluteError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanAbsoluteError [immutability] argset_0", () => {
  const __args = [[], []];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanAbsoluteError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanAbsoluteError [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateMeanAbsoluteError", __args);
  const second = __attempt("calculateMeanAbsoluteError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanAbsoluteError [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanAbsoluteError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanAbsoluteError [determinism] argset_2", () => {
  const __args = [[0], [0]];
  const first = __attempt("calculateMeanAbsoluteError", __args);
  const second = __attempt("calculateMeanAbsoluteError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanAbsoluteError [immutability] argset_2", () => {
  const __args = [[0], [0]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanAbsoluteError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanAbsoluteError [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateMeanAbsoluteError", __args);
  const second = __attempt("calculateMeanAbsoluteError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanAbsoluteError [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanAbsoluteError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanAbsoluteError [edge_safety] edge_0", () => {
  const __args = [[], []];
  try {
    __call("calculateMeanAbsoluteError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanAbsoluteError [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2]];
  try {
    __call("calculateMeanAbsoluteError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanAbsoluteError [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2]];
  try {
    __call("calculateMeanAbsoluteError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanAbsoluteError [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], []];
  try {
    __call("calculateMeanAbsoluteError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanAbsoluteError [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null]];
  try {
    __call("calculateMeanAbsoluteError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanSquaredError [determinism] argset_0", () => {
  const __args = [[], []];
  const first = __attempt("calculateMeanSquaredError", __args);
  const second = __attempt("calculateMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanSquaredError [immutability] argset_0", () => {
  const __args = [[], []];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanSquaredError [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateMeanSquaredError", __args);
  const second = __attempt("calculateMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanSquaredError [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanSquaredError [determinism] argset_2", () => {
  const __args = [[0], [0]];
  const first = __attempt("calculateMeanSquaredError", __args);
  const second = __attempt("calculateMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanSquaredError [immutability] argset_2", () => {
  const __args = [[0], [0]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanSquaredError [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateMeanSquaredError", __args);
  const second = __attempt("calculateMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateMeanSquaredError [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateMeanSquaredError [edge_safety] edge_0", () => {
  const __args = [[], []];
  try {
    __call("calculateMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanSquaredError [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2]];
  try {
    __call("calculateMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanSquaredError [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2]];
  try {
    __call("calculateMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanSquaredError [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], []];
  try {
    __call("calculateMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateMeanSquaredError [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null]];
  try {
    __call("calculateMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateRootMeanSquaredError [determinism] argset_0", () => {
  const __args = [[], []];
  const first = __attempt("calculateRootMeanSquaredError", __args);
  const second = __attempt("calculateRootMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateRootMeanSquaredError [immutability] argset_0", () => {
  const __args = [[], []];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateRootMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateRootMeanSquaredError [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateRootMeanSquaredError", __args);
  const second = __attempt("calculateRootMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateRootMeanSquaredError [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateRootMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateRootMeanSquaredError [determinism] argset_2", () => {
  const __args = [[0], [0]];
  const first = __attempt("calculateRootMeanSquaredError", __args);
  const second = __attempt("calculateRootMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateRootMeanSquaredError [immutability] argset_2", () => {
  const __args = [[0], [0]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateRootMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateRootMeanSquaredError [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateRootMeanSquaredError", __args);
  const second = __attempt("calculateRootMeanSquaredError", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateRootMeanSquaredError [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateRootMeanSquaredError", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateRootMeanSquaredError [edge_safety] edge_0", () => {
  const __args = [[], []];
  try {
    __call("calculateRootMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateRootMeanSquaredError [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2]];
  try {
    __call("calculateRootMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateRootMeanSquaredError [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2]];
  try {
    __call("calculateRootMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateRootMeanSquaredError [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], []];
  try {
    __call("calculateRootMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateRootMeanSquaredError [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null]];
  try {
    __call("calculateRootMeanSquaredError", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateR2Score [determinism] argset_0", () => {
  const __args = [[], []];
  const first = __attempt("calculateR2Score", __args);
  const second = __attempt("calculateR2Score", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateR2Score [immutability] argset_0", () => {
  const __args = [[], []];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateR2Score", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateR2Score [determinism] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateR2Score", __args);
  const second = __attempt("calculateR2Score", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateR2Score [immutability] argset_1", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateR2Score", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateR2Score [determinism] argset_2", () => {
  const __args = [[0], [0]];
  const first = __attempt("calculateR2Score", __args);
  const second = __attempt("calculateR2Score", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateR2Score [immutability] argset_2", () => {
  const __args = [[0], [0]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateR2Score", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateR2Score [determinism] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const first = __attempt("calculateR2Score", __args);
  const second = __attempt("calculateR2Score", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("calculateR2Score [immutability] argset_3", () => {
  const __args = [[3, 1, 2], [3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("calculateR2Score", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("calculateR2Score [edge_safety] edge_0", () => {
  const __args = [[], []];
  try {
    __call("calculateR2Score", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateR2Score [edge_safety] edge_1", () => {
  const __args = [[], [3, 1, 2]];
  try {
    __call("calculateR2Score", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateR2Score [edge_safety] edge_2", () => {
  const __args = [[null], [3, 1, 2]];
  try {
    __call("calculateR2Score", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateR2Score [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], []];
  try {
    __call("calculateR2Score", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("calculateR2Score [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], [null]];
  try {
    __call("calculateR2Score", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

