// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_traverse_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_traverse_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_traverse_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

function __call(name, args) {
  if (typeof mod[name] !== "function") throw new Error("missing export " + name);
  return mod[name].apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("flatten_tree [determinism] argset_0", () => {
  const __args = [[], "alpha"];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_0", () => {
  const __args = [[], "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_0", () => {
  const __args = [[], "alpha"];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][2];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("flatten_tree [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][5];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("flatten_tree [determinism] argset_2", () => {
  const __args = [[0], ""];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_2", () => {
  const __args = [[0], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_2", () => {
  const __args = [[0], ""];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][8];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("flatten_tree [determinism] argset_3", () => {
  const __args = [[], "  spaced  "];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_3", () => {
  const __args = [[], "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_3", () => {
  const __args = [[], "  spaced  "];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][11];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("flatten_tree [determinism] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][14];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("flatten_tree [determinism] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const first = __attempt("flatten_tree", __args);
  const second = __attempt("flatten_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("flatten_tree [immutability] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("flatten_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("flatten_tree [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const actual = JSON.stringify(__attempt("flatten_tree", __args));
  const stored = __snapshots["flatten_tree"] && __snapshots["flatten_tree"][17];
  if (__capture || stored === undefined) {
    __snapshots["flatten_tree"] = __snapshots["flatten_tree"] || {};
    __snapshots["flatten_tree"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("flatten_tree [edge_safety] edge_0", () => {
  const __args = [[], ""];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_1", () => {
  const __args = [[null], ""];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], ""];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], " "];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], "\t"];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "0"];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("flatten_tree [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "null"];
  try {
    __call("flatten_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [determinism] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][2];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("get_depth [determinism] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][5];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("get_depth [determinism] argset_2", () => {
  const __args = ["seed_value", ""];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_2", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_2", () => {
  const __args = ["seed_value", ""];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][8];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("get_depth [determinism] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][11];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("get_depth [determinism] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][14];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("get_depth [determinism] argset_5", () => {
  const __args = ["seed_value", ""];
  const first = __attempt("get_depth", __args);
  const second = __attempt("get_depth", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("get_depth [immutability] argset_5", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("get_depth", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("get_depth [snapshot] argset_5", () => {
  const __args = ["seed_value", ""];
  const actual = JSON.stringify(__attempt("get_depth", __args));
  const stored = __snapshots["get_depth"] && __snapshots["get_depth"][17];
  if (__capture || stored === undefined) {
    __snapshots["get_depth"] = __snapshots["get_depth"] || {};
    __snapshots["get_depth"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("get_depth [edge_safety] edge_0", () => {
  const __args = [null, ""];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_1", () => {
  const __args = [undefined, ""];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_2", () => {
  const __args = ["seed_value", ""];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_3", () => {
  const __args = ["seed_value", " "];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_4", () => {
  const __args = ["seed_value", "\t"];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_5", () => {
  const __args = ["seed_value", "0"];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("get_depth [edge_safety] edge_6", () => {
  const __args = ["seed_value", "null"];
  try {
    __call("get_depth", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value", "alpha"];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value", "alpha"];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][2];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("walk_tree [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value", "hello world"];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value", "hello world"];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][5];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("walk_tree [determinism] argset_2", () => {
  const __args = ["seed_value", "seed_value", ""];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_2", () => {
  const __args = ["seed_value", "seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_2", () => {
  const __args = ["seed_value", "seed_value", ""];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][8];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("walk_tree [determinism] argset_3", () => {
  const __args = ["seed_value", "seed_value", "  spaced  "];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_3", () => {
  const __args = ["seed_value", "seed_value", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_3", () => {
  const __args = ["seed_value", "seed_value", "  spaced  "];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][11];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("walk_tree [determinism] argset_4", () => {
  const __args = ["seed_value", "seed_value", "Alpha42"];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_4", () => {
  const __args = ["seed_value", "seed_value", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_4", () => {
  const __args = ["seed_value", "seed_value", "Alpha42"];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][14];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("walk_tree [determinism] argset_5", () => {
  const __args = ["seed_value", "seed_value", ""];
  const first = __attempt("walk_tree", __args);
  const second = __attempt("walk_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("walk_tree [immutability] argset_5", () => {
  const __args = ["seed_value", "seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("walk_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("walk_tree [snapshot] argset_5", () => {
  const __args = ["seed_value", "seed_value", ""];
  const actual = JSON.stringify(__attempt("walk_tree", __args));
  const stored = __snapshots["walk_tree"] && __snapshots["walk_tree"][17];
  if (__capture || stored === undefined) {
    __snapshots["walk_tree"] = __snapshots["walk_tree"] || {};
    __snapshots["walk_tree"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("walk_tree [edge_safety] edge_0", () => {
  const __args = [null, null, ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_1", () => {
  const __args = [null, "seed_value", ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value", ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_3", () => {
  const __args = ["seed_value", null, ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined, ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_5", () => {
  const __args = ["seed_value", "seed_value", ""];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_6", () => {
  const __args = ["seed_value", "seed_value", " "];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_7", () => {
  const __args = ["seed_value", "seed_value", "\t"];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_8", () => {
  const __args = ["seed_value", "seed_value", "0"];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("walk_tree [edge_safety] edge_9", () => {
  const __args = ["seed_value", "seed_value", "null"];
  try {
    __call("walk_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [determinism] argset_0", () => {
  const __args = [[], (function stub(value) { return value; }), "alpha"];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_0", () => {
  const __args = [[], (function stub(value) { return value; }), "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_0", () => {
  const __args = [[], (function stub(value) { return value; }), "alpha"];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][2];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("filter_tree [determinism] argset_1", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "hello world"];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_1", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "hello world"];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][5];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("filter_tree [determinism] argset_2", () => {
  const __args = [[0], (function stub(value) { return value; }), ""];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_2", () => {
  const __args = [[0], (function stub(value) { return value; }), ""];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_2", () => {
  const __args = [[0], (function stub(value) { return value; }), ""];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][8];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("filter_tree [determinism] argset_3", () => {
  const __args = [[], (function stub(value) { return value; }), "  spaced  "];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_3", () => {
  const __args = [[], (function stub(value) { return value; }), "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_3", () => {
  const __args = [[], (function stub(value) { return value; }), "  spaced  "];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][11];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("filter_tree [determinism] argset_4", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "Alpha42"];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_4", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "Alpha42"];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][14];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("filter_tree [determinism] argset_5", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), ""];
  const first = __attempt("filter_tree", __args);
  const second = __attempt("filter_tree", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("filter_tree [immutability] argset_5", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), ""];
  const before = JSON.stringify(__args);
  const result = __attempt("filter_tree", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("filter_tree [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), ""];
  const actual = JSON.stringify(__attempt("filter_tree", __args));
  const stored = __snapshots["filter_tree"] && __snapshots["filter_tree"][17];
  if (__capture || stored === undefined) {
    __snapshots["filter_tree"] = __snapshots["filter_tree"] || {};
    __snapshots["filter_tree"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("filter_tree [edge_safety] edge_0", () => {
  const __args = [[], (function stub(value) { return value; }), ""];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_1", () => {
  const __args = [[null], (function stub(value) { return value; }), ""];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), ""];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), " "];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "\t"];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "0"];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("filter_tree [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], (function stub(value) { return value; }), "null"];
  try {
    __call("filter_tree", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

