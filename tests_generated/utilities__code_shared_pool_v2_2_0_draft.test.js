// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_pool_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_pool_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_pool_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

function __call(name, args) {
  if (typeof mod[name] !== "function") throw new Error("missing export " + name);
  return mod[name].apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("run_parallel [determinism] argset_0", () => {
  const __args = [[], 0];
  const first = __attempt("run_parallel", __args);
  const second = __attempt("run_parallel", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_parallel [immutability] argset_0", () => {
  const __args = [[], 0];
  const before = JSON.stringify(__args);
  const result = __attempt("run_parallel", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_parallel [snapshot] argset_0", () => {
  const __args = [[], 0];
  const actual = JSON.stringify(__attempt("run_parallel", __args));
  const stored = __snapshots["run_parallel"] && __snapshots["run_parallel"][2];
  if (__capture || stored === undefined) {
    __snapshots["run_parallel"] = __snapshots["run_parallel"] || {};
    __snapshots["run_parallel"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("run_parallel [determinism] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const first = __attempt("run_parallel", __args);
  const second = __attempt("run_parallel", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_parallel [immutability] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const before = JSON.stringify(__args);
  const result = __attempt("run_parallel", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_parallel [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], -1];
  const actual = JSON.stringify(__attempt("run_parallel", __args));
  const stored = __snapshots["run_parallel"] && __snapshots["run_parallel"][5];
  if (__capture || stored === undefined) {
    __snapshots["run_parallel"] = __snapshots["run_parallel"] || {};
    __snapshots["run_parallel"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("run_parallel [determinism] argset_2", () => {
  const __args = [[0], 42];
  const first = __attempt("run_parallel", __args);
  const second = __attempt("run_parallel", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_parallel [immutability] argset_2", () => {
  const __args = [[0], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("run_parallel", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_parallel [snapshot] argset_2", () => {
  const __args = [[0], 42];
  const actual = JSON.stringify(__attempt("run_parallel", __args));
  const stored = __snapshots["run_parallel"] && __snapshots["run_parallel"][8];
  if (__capture || stored === undefined) {
    __snapshots["run_parallel"] = __snapshots["run_parallel"] || {};
    __snapshots["run_parallel"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("run_parallel [determinism] argset_3", () => {
  const __args = [[], 3.14];
  const first = __attempt("run_parallel", __args);
  const second = __attempt("run_parallel", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_parallel [immutability] argset_3", () => {
  const __args = [[], 3.14];
  const before = JSON.stringify(__args);
  const result = __attempt("run_parallel", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_parallel [snapshot] argset_3", () => {
  const __args = [[], 3.14];
  const actual = JSON.stringify(__attempt("run_parallel", __args));
  const stored = __snapshots["run_parallel"] && __snapshots["run_parallel"][11];
  if (__capture || stored === undefined) {
    __snapshots["run_parallel"] = __snapshots["run_parallel"] || {};
    __snapshots["run_parallel"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("run_parallel [determinism] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const first = __attempt("run_parallel", __args);
  const second = __attempt("run_parallel", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_parallel [immutability] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("run_parallel", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_parallel [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], 42];
  const actual = JSON.stringify(__attempt("run_parallel", __args));
  const stored = __snapshots["run_parallel"] && __snapshots["run_parallel"][14];
  if (__capture || stored === undefined) {
    __snapshots["run_parallel"] = __snapshots["run_parallel"] || {};
    __snapshots["run_parallel"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("run_parallel [edge_safety] edge_0", () => {
  const __args = [[], 0];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_1", () => {
  const __args = [[], 42];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_2", () => {
  const __args = [[null], 42];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], 0];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], -1];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], NaN];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], Infinity];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_7", () => {
  const __args = [[3, 1, 2], -Infinity];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_parallel [edge_safety] edge_8", () => {
  const __args = [[3, 1, 2], 65536];
  try {
    __call("run_parallel", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [determinism] argset_0", () => {
  const __args = [(function stub(value) { return value; }), [], 0];
  const first = __attempt("run_with_limit", __args);
  const second = __attempt("run_with_limit", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_with_limit [immutability] argset_0", () => {
  const __args = [(function stub(value) { return value; }), [], 0];
  const before = JSON.stringify(__args);
  const result = __attempt("run_with_limit", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_with_limit [snapshot] argset_0", () => {
  const __args = [(function stub(value) { return value; }), [], 0];
  const actual = JSON.stringify(__attempt("run_with_limit", __args));
  const stored = __snapshots["run_with_limit"] && __snapshots["run_with_limit"][2];
  if (__capture || stored === undefined) {
    __snapshots["run_with_limit"] = __snapshots["run_with_limit"] || {};
    __snapshots["run_with_limit"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("run_with_limit [determinism] argset_1", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], -1];
  const first = __attempt("run_with_limit", __args);
  const second = __attempt("run_with_limit", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_with_limit [immutability] argset_1", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], -1];
  const before = JSON.stringify(__args);
  const result = __attempt("run_with_limit", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_with_limit [snapshot] argset_1", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], -1];
  const actual = JSON.stringify(__attempt("run_with_limit", __args));
  const stored = __snapshots["run_with_limit"] && __snapshots["run_with_limit"][5];
  if (__capture || stored === undefined) {
    __snapshots["run_with_limit"] = __snapshots["run_with_limit"] || {};
    __snapshots["run_with_limit"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("run_with_limit [determinism] argset_2", () => {
  const __args = [(function stub(value) { return value; }), [0], 42];
  const first = __attempt("run_with_limit", __args);
  const second = __attempt("run_with_limit", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_with_limit [immutability] argset_2", () => {
  const __args = [(function stub(value) { return value; }), [0], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("run_with_limit", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_with_limit [snapshot] argset_2", () => {
  const __args = [(function stub(value) { return value; }), [0], 42];
  const actual = JSON.stringify(__attempt("run_with_limit", __args));
  const stored = __snapshots["run_with_limit"] && __snapshots["run_with_limit"][8];
  if (__capture || stored === undefined) {
    __snapshots["run_with_limit"] = __snapshots["run_with_limit"] || {};
    __snapshots["run_with_limit"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("run_with_limit [determinism] argset_3", () => {
  const __args = [(function stub(value) { return value; }), [], 3.14];
  const first = __attempt("run_with_limit", __args);
  const second = __attempt("run_with_limit", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_with_limit [immutability] argset_3", () => {
  const __args = [(function stub(value) { return value; }), [], 3.14];
  const before = JSON.stringify(__args);
  const result = __attempt("run_with_limit", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_with_limit [snapshot] argset_3", () => {
  const __args = [(function stub(value) { return value; }), [], 3.14];
  const actual = JSON.stringify(__attempt("run_with_limit", __args));
  const stored = __snapshots["run_with_limit"] && __snapshots["run_with_limit"][11];
  if (__capture || stored === undefined) {
    __snapshots["run_with_limit"] = __snapshots["run_with_limit"] || {};
    __snapshots["run_with_limit"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("run_with_limit [determinism] argset_4", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 42];
  const first = __attempt("run_with_limit", __args);
  const second = __attempt("run_with_limit", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("run_with_limit [immutability] argset_4", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 42];
  const before = JSON.stringify(__args);
  const result = __attempt("run_with_limit", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("run_with_limit [snapshot] argset_4", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 42];
  const actual = JSON.stringify(__attempt("run_with_limit", __args));
  const stored = __snapshots["run_with_limit"] && __snapshots["run_with_limit"][14];
  if (__capture || stored === undefined) {
    __snapshots["run_with_limit"] = __snapshots["run_with_limit"] || {};
    __snapshots["run_with_limit"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("run_with_limit [edge_safety] edge_0", () => {
  const __args = [(function stub(value) { return value; }), [], 0];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_1", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 42];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_2", () => {
  const __args = [(function stub(value) { return value; }), [], 42];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_3", () => {
  const __args = [(function stub(value) { return value; }), [null], 42];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_4", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 0];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_5", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], -1];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_6", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], NaN];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_7", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], Infinity];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_8", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], -Infinity];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("run_with_limit [edge_safety] edge_9", () => {
  const __args = [(function stub(value) { return value; }), [3, 1, 2], 65536];
  try {
    __call("run_with_limit", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

