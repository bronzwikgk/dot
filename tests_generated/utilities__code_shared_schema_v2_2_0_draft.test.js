// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_schema_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_schema_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_schema_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

function __call(name, args) {
  if (typeof mod[name] !== "function") throw new Error("missing export " + name);
  return mod[name].apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("validate_against_schema [determinism] argset_0", () => {
  const __args = [{}, "seed_value"];
  const first = __attempt("validate_against_schema", __args);
  const second = __attempt("validate_against_schema", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validate_against_schema [immutability] argset_0", () => {
  const __args = [{}, "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validate_against_schema", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validate_against_schema [snapshot] argset_0", () => {
  const __args = [{}, "seed_value"];
  const actual = JSON.stringify(__attempt("validate_against_schema", __args));
  const stored = __snapshots["validate_against_schema"] && __snapshots["validate_against_schema"][2];
  if (__capture || stored === undefined) {
    __snapshots["validate_against_schema"] = __snapshots["validate_against_schema"] || {};
    __snapshots["validate_against_schema"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("validate_against_schema [determinism] argset_1", () => {
  const __args = [{"a": 1}, "seed_value"];
  const first = __attempt("validate_against_schema", __args);
  const second = __attempt("validate_against_schema", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validate_against_schema [immutability] argset_1", () => {
  const __args = [{"a": 1}, "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validate_against_schema", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validate_against_schema [snapshot] argset_1", () => {
  const __args = [{"a": 1}, "seed_value"];
  const actual = JSON.stringify(__attempt("validate_against_schema", __args));
  const stored = __snapshots["validate_against_schema"] && __snapshots["validate_against_schema"][5];
  if (__capture || stored === undefined) {
    __snapshots["validate_against_schema"] = __snapshots["validate_against_schema"] || {};
    __snapshots["validate_against_schema"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("validate_against_schema [determinism] argset_2", () => {
  const __args = [{"a": 1}, "seed_value"];
  const first = __attempt("validate_against_schema", __args);
  const second = __attempt("validate_against_schema", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("validate_against_schema [immutability] argset_2", () => {
  const __args = [{"a": 1}, "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("validate_against_schema", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("validate_against_schema [snapshot] argset_2", () => {
  const __args = [{"a": 1}, "seed_value"];
  const actual = JSON.stringify(__attempt("validate_against_schema", __args));
  const stored = __snapshots["validate_against_schema"] && __snapshots["validate_against_schema"][8];
  if (__capture || stored === undefined) {
    __snapshots["validate_against_schema"] = __snapshots["validate_against_schema"] || {};
    __snapshots["validate_against_schema"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("validate_against_schema [edge_safety] edge_0", () => {
  const __args = [{}, null];
  try {
    __call("validate_against_schema", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate_against_schema [edge_safety] edge_1", () => {
  const __args = [{}, "seed_value"];
  try {
    __call("validate_against_schema", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate_against_schema [edge_safety] edge_2", () => {
  const __args = [null, "seed_value"];
  try {
    __call("validate_against_schema", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate_against_schema [edge_safety] edge_3", () => {
  const __args = [{"a": 1}, null];
  try {
    __call("validate_against_schema", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("validate_against_schema [edge_safety] edge_4", () => {
  const __args = [{"a": 1}, undefined];
  try {
    __call("validate_against_schema", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("merge_schemas [determinism] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("merge_schemas", __args);
  const second = __attempt("merge_schemas", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("merge_schemas [immutability] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("merge_schemas", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("merge_schemas [snapshot] argset_0", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("merge_schemas", __args));
  const stored = __snapshots["merge_schemas"] && __snapshots["merge_schemas"][2];
  if (__capture || stored === undefined) {
    __snapshots["merge_schemas"] = __snapshots["merge_schemas"] || {};
    __snapshots["merge_schemas"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("merge_schemas [determinism] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const first = __attempt("merge_schemas", __args);
  const second = __attempt("merge_schemas", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("merge_schemas [immutability] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("merge_schemas", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("merge_schemas [snapshot] argset_1", () => {
  const __args = ["seed_value", "seed_value"];
  const actual = JSON.stringify(__attempt("merge_schemas", __args));
  const stored = __snapshots["merge_schemas"] && __snapshots["merge_schemas"][5];
  if (__capture || stored === undefined) {
    __snapshots["merge_schemas"] = __snapshots["merge_schemas"] || {};
    __snapshots["merge_schemas"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("merge_schemas [edge_safety] edge_0", () => {
  const __args = [null, null];
  try {
    __call("merge_schemas", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("merge_schemas [edge_safety] edge_1", () => {
  const __args = [null, "seed_value"];
  try {
    __call("merge_schemas", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("merge_schemas [edge_safety] edge_2", () => {
  const __args = [undefined, "seed_value"];
  try {
    __call("merge_schemas", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("merge_schemas [edge_safety] edge_3", () => {
  const __args = ["seed_value", null];
  try {
    __call("merge_schemas", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("merge_schemas [edge_safety] edge_4", () => {
  const __args = ["seed_value", undefined];
  try {
    __call("merge_schemas", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

