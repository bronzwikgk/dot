// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_cli_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\plugins\\code_shared_cli_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/plugins__code_shared_cli_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

function __call(name, args) {
  if (typeof mod[name] !== "function") throw new Error("missing export " + name);
  return mod[name].apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("buildMenuLines [determinism] argset_0", () => {
  const __args = [];
  const first = __attempt("buildMenuLines", __args);
  const second = __attempt("buildMenuLines", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("buildMenuLines [edge_safety] edge_0", () => {
  const __args = [];
  try {
    __call("buildMenuLines", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseCliRequest [determinism] argset_0", () => {
  const __args = [[]];
  const first = __attempt("parseCliRequest", __args);
  const second = __attempt("parseCliRequest", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseCliRequest [immutability] argset_0", () => {
  const __args = [[]];
  const before = JSON.stringify(__args);
  const result = __attempt("parseCliRequest", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseCliRequest [snapshot] argset_0", () => {
  const __args = [[]];
  const actual = JSON.stringify(__attempt("parseCliRequest", __args));
  const stored = __snapshots["parseCliRequest"] && __snapshots["parseCliRequest"][2];
  if (__capture || stored === undefined) {
    __snapshots["parseCliRequest"] = __snapshots["parseCliRequest"] || {};
    __snapshots["parseCliRequest"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("parseCliRequest [determinism] argset_1", () => {
  const __args = [[3, 1, 2]];
  const first = __attempt("parseCliRequest", __args);
  const second = __attempt("parseCliRequest", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseCliRequest [immutability] argset_1", () => {
  const __args = [[3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("parseCliRequest", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseCliRequest [snapshot] argset_1", () => {
  const __args = [[3, 1, 2]];
  const actual = JSON.stringify(__attempt("parseCliRequest", __args));
  const stored = __snapshots["parseCliRequest"] && __snapshots["parseCliRequest"][5];
  if (__capture || stored === undefined) {
    __snapshots["parseCliRequest"] = __snapshots["parseCliRequest"] || {};
    __snapshots["parseCliRequest"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("parseCliRequest [determinism] argset_2", () => {
  const __args = [[0]];
  const first = __attempt("parseCliRequest", __args);
  const second = __attempt("parseCliRequest", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseCliRequest [immutability] argset_2", () => {
  const __args = [[0]];
  const before = JSON.stringify(__args);
  const result = __attempt("parseCliRequest", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseCliRequest [snapshot] argset_2", () => {
  const __args = [[0]];
  const actual = JSON.stringify(__attempt("parseCliRequest", __args));
  const stored = __snapshots["parseCliRequest"] && __snapshots["parseCliRequest"][8];
  if (__capture || stored === undefined) {
    __snapshots["parseCliRequest"] = __snapshots["parseCliRequest"] || {};
    __snapshots["parseCliRequest"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("parseCliRequest [determinism] argset_3", () => {
  const __args = [[3, 1, 2]];
  const first = __attempt("parseCliRequest", __args);
  const second = __attempt("parseCliRequest", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseCliRequest [immutability] argset_3", () => {
  const __args = [[3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("parseCliRequest", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseCliRequest [snapshot] argset_3", () => {
  const __args = [[3, 1, 2]];
  const actual = JSON.stringify(__attempt("parseCliRequest", __args));
  const stored = __snapshots["parseCliRequest"] && __snapshots["parseCliRequest"][11];
  if (__capture || stored === undefined) {
    __snapshots["parseCliRequest"] = __snapshots["parseCliRequest"] || {};
    __snapshots["parseCliRequest"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("parseCliRequest [edge_safety] edge_0", () => {
  const __args = [[]];
  try {
    __call("parseCliRequest", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseCliRequest [edge_safety] edge_1", () => {
  const __args = [[null]];
  try {
    __call("parseCliRequest", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("loadInputFromArgs [determinism] argset_0", () => {
  const __args = [[]];
  const first = __attempt("loadInputFromArgs", __args);
  const second = __attempt("loadInputFromArgs", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("loadInputFromArgs [immutability] argset_0", () => {
  const __args = [[]];
  const before = JSON.stringify(__args);
  const result = __attempt("loadInputFromArgs", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("loadInputFromArgs [snapshot] argset_0", () => {
  const __args = [[]];
  const actual = JSON.stringify(__attempt("loadInputFromArgs", __args));
  const stored = __snapshots["loadInputFromArgs"] && __snapshots["loadInputFromArgs"][2];
  if (__capture || stored === undefined) {
    __snapshots["loadInputFromArgs"] = __snapshots["loadInputFromArgs"] || {};
    __snapshots["loadInputFromArgs"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("loadInputFromArgs [determinism] argset_1", () => {
  const __args = [[3, 1, 2]];
  const first = __attempt("loadInputFromArgs", __args);
  const second = __attempt("loadInputFromArgs", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("loadInputFromArgs [immutability] argset_1", () => {
  const __args = [[3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("loadInputFromArgs", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("loadInputFromArgs [snapshot] argset_1", () => {
  const __args = [[3, 1, 2]];
  const actual = JSON.stringify(__attempt("loadInputFromArgs", __args));
  const stored = __snapshots["loadInputFromArgs"] && __snapshots["loadInputFromArgs"][5];
  if (__capture || stored === undefined) {
    __snapshots["loadInputFromArgs"] = __snapshots["loadInputFromArgs"] || {};
    __snapshots["loadInputFromArgs"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("loadInputFromArgs [determinism] argset_2", () => {
  const __args = [[0]];
  const first = __attempt("loadInputFromArgs", __args);
  const second = __attempt("loadInputFromArgs", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("loadInputFromArgs [immutability] argset_2", () => {
  const __args = [[0]];
  const before = JSON.stringify(__args);
  const result = __attempt("loadInputFromArgs", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("loadInputFromArgs [snapshot] argset_2", () => {
  const __args = [[0]];
  const actual = JSON.stringify(__attempt("loadInputFromArgs", __args));
  const stored = __snapshots["loadInputFromArgs"] && __snapshots["loadInputFromArgs"][8];
  if (__capture || stored === undefined) {
    __snapshots["loadInputFromArgs"] = __snapshots["loadInputFromArgs"] || {};
    __snapshots["loadInputFromArgs"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("loadInputFromArgs [determinism] argset_3", () => {
  const __args = [[3, 1, 2]];
  const first = __attempt("loadInputFromArgs", __args);
  const second = __attempt("loadInputFromArgs", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("loadInputFromArgs [immutability] argset_3", () => {
  const __args = [[3, 1, 2]];
  const before = JSON.stringify(__args);
  const result = __attempt("loadInputFromArgs", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("loadInputFromArgs [snapshot] argset_3", () => {
  const __args = [[3, 1, 2]];
  const actual = JSON.stringify(__attempt("loadInputFromArgs", __args));
  const stored = __snapshots["loadInputFromArgs"] && __snapshots["loadInputFromArgs"][11];
  if (__capture || stored === undefined) {
    __snapshots["loadInputFromArgs"] = __snapshots["loadInputFromArgs"] || {};
    __snapshots["loadInputFromArgs"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("loadInputFromArgs [edge_safety] edge_0", () => {
  const __args = [[]];
  try {
    __call("loadInputFromArgs", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("loadInputFromArgs [edge_safety] edge_1", () => {
  const __args = [[null]];
  try {
    __call("loadInputFromArgs", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("inferEntityFromCommand [determinism] argset_0", () => {
  const __args = ["seed_value"];
  const first = __attempt("inferEntityFromCommand", __args);
  const second = __attempt("inferEntityFromCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("inferEntityFromCommand [immutability] argset_0", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("inferEntityFromCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("inferEntityFromCommand [snapshot] argset_0", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("inferEntityFromCommand", __args));
  const stored = __snapshots["inferEntityFromCommand"] && __snapshots["inferEntityFromCommand"][2];
  if (__capture || stored === undefined) {
    __snapshots["inferEntityFromCommand"] = __snapshots["inferEntityFromCommand"] || {};
    __snapshots["inferEntityFromCommand"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("inferEntityFromCommand [determinism] argset_1", () => {
  const __args = ["seed_value"];
  const first = __attempt("inferEntityFromCommand", __args);
  const second = __attempt("inferEntityFromCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("inferEntityFromCommand [immutability] argset_1", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("inferEntityFromCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("inferEntityFromCommand [snapshot] argset_1", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("inferEntityFromCommand", __args));
  const stored = __snapshots["inferEntityFromCommand"] && __snapshots["inferEntityFromCommand"][5];
  if (__capture || stored === undefined) {
    __snapshots["inferEntityFromCommand"] = __snapshots["inferEntityFromCommand"] || {};
    __snapshots["inferEntityFromCommand"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("inferEntityFromCommand [edge_safety] edge_0", () => {
  const __args = [null];
  try {
    __call("inferEntityFromCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("inferEntityFromCommand [edge_safety] edge_1", () => {
  const __args = [undefined];
  try {
    __call("inferEntityFromCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [immutability] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [immutability] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [immutability] argset_2", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [immutability] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [immutability] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [immutability] argset_5", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("generateActivitiesFromReport", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("generateActivitiesFromReport [edge_safety] edge_0", () => {
  const __args = [null, ""];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_1", () => {
  const __args = [undefined, ""];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_2", () => {
  const __args = ["seed_value", ""];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_3", () => {
  const __args = ["seed_value", " "];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_4", () => {
  const __args = ["seed_value", "\t"];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_5", () => {
  const __args = ["seed_value", "0"];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("generateActivitiesFromReport [edge_safety] edge_6", () => {
  const __args = ["seed_value", "null"];
  try {
    __call("generateActivitiesFromReport", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseSimpleCommand [determinism] argset_0", () => {
  const __args = ["alpha"];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_0", () => {
  const __args = ["alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_0", () => {
  const __args = ["alpha"];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][2];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("parseSimpleCommand [determinism] argset_1", () => {
  const __args = ["hello world"];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_1", () => {
  const __args = ["hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_1", () => {
  const __args = ["hello world"];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][5];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("parseSimpleCommand [determinism] argset_2", () => {
  const __args = [""];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_2", () => {
  const __args = [""];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_2", () => {
  const __args = [""];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][8];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("parseSimpleCommand [determinism] argset_3", () => {
  const __args = ["  spaced  "];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_3", () => {
  const __args = ["  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_3", () => {
  const __args = ["  spaced  "];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][11];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("parseSimpleCommand [determinism] argset_4", () => {
  const __args = ["Alpha42"];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_4", () => {
  const __args = ["Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_4", () => {
  const __args = ["Alpha42"];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][14];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("parseSimpleCommand [determinism] argset_5", () => {
  const __args = [""];
  const first = __attempt("parseSimpleCommand", __args);
  const second = __attempt("parseSimpleCommand", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseSimpleCommand [immutability] argset_5", () => {
  const __args = [""];
  const before = JSON.stringify(__args);
  const result = __attempt("parseSimpleCommand", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseSimpleCommand [snapshot] argset_5", () => {
  const __args = [""];
  const actual = JSON.stringify(__attempt("parseSimpleCommand", __args));
  const stored = __snapshots["parseSimpleCommand"] && __snapshots["parseSimpleCommand"][17];
  if (__capture || stored === undefined) {
    __snapshots["parseSimpleCommand"] = __snapshots["parseSimpleCommand"] || {};
    __snapshots["parseSimpleCommand"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("parseSimpleCommand [edge_safety] edge_0", () => {
  const __args = [""];
  try {
    __call("parseSimpleCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseSimpleCommand [edge_safety] edge_1", () => {
  const __args = [" "];
  try {
    __call("parseSimpleCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseSimpleCommand [edge_safety] edge_2", () => {
  const __args = ["\t"];
  try {
    __call("parseSimpleCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseSimpleCommand [edge_safety] edge_3", () => {
  const __args = ["0"];
  try {
    __call("parseSimpleCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseSimpleCommand [edge_safety] edge_4", () => {
  const __args = ["null"];
  try {
    __call("parseSimpleCommand", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseMenuDefinitions [determinism] argset_0", () => {
  const __args = ["alpha"];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_0", () => {
  const __args = ["alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_0", () => {
  const __args = ["alpha"];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][2];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("parseMenuDefinitions [determinism] argset_1", () => {
  const __args = ["hello world"];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_1", () => {
  const __args = ["hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_1", () => {
  const __args = ["hello world"];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][5];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("parseMenuDefinitions [determinism] argset_2", () => {
  const __args = [""];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_2", () => {
  const __args = [""];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_2", () => {
  const __args = [""];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][8];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("parseMenuDefinitions [determinism] argset_3", () => {
  const __args = ["  spaced  "];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_3", () => {
  const __args = ["  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_3", () => {
  const __args = ["  spaced  "];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][11];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("parseMenuDefinitions [determinism] argset_4", () => {
  const __args = ["Alpha42"];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_4", () => {
  const __args = ["Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_4", () => {
  const __args = ["Alpha42"];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][14];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("parseMenuDefinitions [determinism] argset_5", () => {
  const __args = [""];
  const first = __attempt("parseMenuDefinitions", __args);
  const second = __attempt("parseMenuDefinitions", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("parseMenuDefinitions [immutability] argset_5", () => {
  const __args = [""];
  const before = JSON.stringify(__args);
  const result = __attempt("parseMenuDefinitions", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("parseMenuDefinitions [snapshot] argset_5", () => {
  const __args = [""];
  const actual = JSON.stringify(__attempt("parseMenuDefinitions", __args));
  const stored = __snapshots["parseMenuDefinitions"] && __snapshots["parseMenuDefinitions"][17];
  if (__capture || stored === undefined) {
    __snapshots["parseMenuDefinitions"] = __snapshots["parseMenuDefinitions"] || {};
    __snapshots["parseMenuDefinitions"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("parseMenuDefinitions [edge_safety] edge_0", () => {
  const __args = [""];
  try {
    __call("parseMenuDefinitions", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseMenuDefinitions [edge_safety] edge_1", () => {
  const __args = [" "];
  try {
    __call("parseMenuDefinitions", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseMenuDefinitions [edge_safety] edge_2", () => {
  const __args = ["\t"];
  try {
    __call("parseMenuDefinitions", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseMenuDefinitions [edge_safety] edge_3", () => {
  const __args = ["0"];
  try {
    __call("parseMenuDefinitions", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("parseMenuDefinitions [edge_safety] edge_4", () => {
  const __args = ["null"];
  try {
    __call("parseMenuDefinitions", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [determinism] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_0", () => {
  const __args = ["seed_value", "alpha"];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][2];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("resolveMenuByPath [determinism] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_1", () => {
  const __args = ["seed_value", "hello world"];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][5];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("resolveMenuByPath [determinism] argset_2", () => {
  const __args = ["seed_value", ""];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_2", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_2", () => {
  const __args = ["seed_value", ""];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][8];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("resolveMenuByPath [determinism] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_3", () => {
  const __args = ["seed_value", "  spaced  "];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][11];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("resolveMenuByPath [determinism] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_4", () => {
  const __args = ["seed_value", "Alpha42"];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][14];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("resolveMenuByPath [determinism] argset_5", () => {
  const __args = ["seed_value", ""];
  const first = __attempt("resolveMenuByPath", __args);
  const second = __attempt("resolveMenuByPath", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("resolveMenuByPath [immutability] argset_5", () => {
  const __args = ["seed_value", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("resolveMenuByPath", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("resolveMenuByPath [snapshot] argset_5", () => {
  const __args = ["seed_value", ""];
  const actual = JSON.stringify(__attempt("resolveMenuByPath", __args));
  const stored = __snapshots["resolveMenuByPath"] && __snapshots["resolveMenuByPath"][17];
  if (__capture || stored === undefined) {
    __snapshots["resolveMenuByPath"] = __snapshots["resolveMenuByPath"] || {};
    __snapshots["resolveMenuByPath"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("resolveMenuByPath [edge_safety] edge_0", () => {
  const __args = [null, ""];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_1", () => {
  const __args = [undefined, ""];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_2", () => {
  const __args = ["seed_value", ""];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_3", () => {
  const __args = ["seed_value", " "];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_4", () => {
  const __args = ["seed_value", "\t"];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_5", () => {
  const __args = ["seed_value", "0"];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("resolveMenuByPath [edge_safety] edge_6", () => {
  const __args = ["seed_value", "null"];
  try {
    __call("resolveMenuByPath", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("renderMenu [determinism] argset_0", () => {
  const __args = ["seed_value"];
  const first = __attempt("renderMenu", __args);
  const second = __attempt("renderMenu", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("renderMenu [immutability] argset_0", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("renderMenu", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("renderMenu [snapshot] argset_0", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("renderMenu", __args));
  const stored = __snapshots["renderMenu"] && __snapshots["renderMenu"][2];
  if (__capture || stored === undefined) {
    __snapshots["renderMenu"] = __snapshots["renderMenu"] || {};
    __snapshots["renderMenu"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("renderMenu [determinism] argset_1", () => {
  const __args = ["seed_value"];
  const first = __attempt("renderMenu", __args);
  const second = __attempt("renderMenu", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("renderMenu [immutability] argset_1", () => {
  const __args = ["seed_value"];
  const before = JSON.stringify(__args);
  const result = __attempt("renderMenu", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("renderMenu [snapshot] argset_1", () => {
  const __args = ["seed_value"];
  const actual = JSON.stringify(__attempt("renderMenu", __args));
  const stored = __snapshots["renderMenu"] && __snapshots["renderMenu"][5];
  if (__capture || stored === undefined) {
    __snapshots["renderMenu"] = __snapshots["renderMenu"] || {};
    __snapshots["renderMenu"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("renderMenu [edge_safety] edge_0", () => {
  const __args = [null];
  try {
    __call("renderMenu", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("renderMenu [edge_safety] edge_1", () => {
  const __args = [undefined];
  try {
    __call("renderMenu", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

