// AUTOGENERATED by pipeline system_validate_and_test_code_shared_v2 for code_shared_collection_v2_2_0_draft
// regenerate instead of editing
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const mod = require("E:\\root_0dot1_wip\\shared_v2\\code\\utilities\\code_shared_collection_v2_2_0_draft.js");
const SNAPSHOT_DIR = __dirname;
const SNAPSHOT_PATH = path.join(SNAPSHOT_DIR, "snapshots/utilities__code_shared_collection_v2_2_0_draft.snap.json");
const __capture = process.env.TESTGEN_CAPTURE === "1";
let __snapshots = {};
if (fs.existsSync(SNAPSHOT_PATH)) {
  __snapshots = JSON.parse(fs.readFileSync(SNAPSHOT_PATH, "utf8"));
}
let __dirty = false;

function __serialize(value) {
  if (value === undefined) return "__undefined__";
  if (typeof value === "number" && !Number.isFinite(value)) return "__" + String(value) + "__";
  if (typeof value === "function") return "__function__";
  try { return JSON.stringify(value); } catch (error) { return "__unserializable__"; }
}

function __attempt(name, args) {
  try {
    return { threw: false, value: __serialize(__call(name, args)) };
  } catch (error) {
    const label = error && error.constructor ? error.constructor.name : String(error);
    return { threw: true, value: "__threw__:" + label + ":" + ((error && error.message) || "") };
  }
}

function __call(name, args) {
  if (typeof mod[name] !== "function") throw new Error("missing export " + name);
  return mod[name].apply(null, args);
}

process.on("exit", function () {
  if (__dirty) fs.writeFileSync(SNAPSHOT_PATH, JSON.stringify(__snapshots, null, 2));
});

test("group_by [determinism] argset_0", () => {
  const __args = [[], "alpha"];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_0", () => {
  const __args = [[], "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_0", () => {
  const __args = [[], "alpha"];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][2];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("group_by [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][5];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("group_by [determinism] argset_2", () => {
  const __args = [[0], ""];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_2", () => {
  const __args = [[0], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_2", () => {
  const __args = [[0], ""];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][8];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("group_by [determinism] argset_3", () => {
  const __args = [[], "  spaced  "];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_3", () => {
  const __args = [[], "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_3", () => {
  const __args = [[], "  spaced  "];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][11];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("group_by [determinism] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][14];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("group_by [determinism] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const first = __attempt("group_by", __args);
  const second = __attempt("group_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("group_by [immutability] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("group_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("group_by [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const actual = JSON.stringify(__attempt("group_by", __args));
  const stored = __snapshots["group_by"] && __snapshots["group_by"][17];
  if (__capture || stored === undefined) {
    __snapshots["group_by"] = __snapshots["group_by"] || {};
    __snapshots["group_by"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("group_by [edge_safety] edge_0", () => {
  const __args = [[], ""];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_1", () => {
  const __args = [[null], ""];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], ""];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], " "];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], "\t"];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "0"];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("group_by [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "null"];
  try {
    __call("group_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [determinism] argset_0", () => {
  const __args = [[], "alpha", "alpha"];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_0", () => {
  const __args = [[], "alpha", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_0", () => {
  const __args = [[], "alpha", "alpha"];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][2];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("sort_by [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "hello world", "hello world"];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "hello world", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "hello world", "hello world"];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][5];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("sort_by [determinism] argset_2", () => {
  const __args = [[0], "", ""];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_2", () => {
  const __args = [[0], "", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_2", () => {
  const __args = [[0], "", ""];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][8];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("sort_by [determinism] argset_3", () => {
  const __args = [[], "  spaced  ", "  spaced  "];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_3", () => {
  const __args = [[], "  spaced  ", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_3", () => {
  const __args = [[], "  spaced  ", "  spaced  "];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][11];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("sort_by [determinism] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42", "Alpha42"];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42", "Alpha42"];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][14];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("sort_by [determinism] argset_5", () => {
  const __args = [[3, 1, 2], "", ""];
  const first = __attempt("sort_by", __args);
  const second = __attempt("sort_by", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("sort_by [immutability] argset_5", () => {
  const __args = [[3, 1, 2], "", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("sort_by", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("sort_by [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], "", ""];
  const actual = JSON.stringify(__attempt("sort_by", __args));
  const stored = __snapshots["sort_by"] && __snapshots["sort_by"][17];
  if (__capture || stored === undefined) {
    __snapshots["sort_by"] = __snapshots["sort_by"] || {};
    __snapshots["sort_by"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("sort_by [edge_safety] edge_0", () => {
  const __args = [[], "", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_1", () => {
  const __args = [[null], "", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], "", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], " ", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], "\t", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "0", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "null", ""];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_7", () => {
  const __args = [[3, 1, 2], "", " "];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_8", () => {
  const __args = [[3, 1, 2], "", "\t"];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_9", () => {
  const __args = [[3, 1, 2], "", "0"];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("sort_by [edge_safety] edge_10", () => {
  const __args = [[3, 1, 2], "", "null"];
  try {
    __call("sort_by", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [determinism] argset_0", () => {
  const __args = [[], "alpha"];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_0", () => {
  const __args = [[], "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_0", () => {
  const __args = [[], "alpha"];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][2];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("unique [determinism] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_1", () => {
  const __args = [[3, 1, 2], "hello world"];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][5];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("unique [determinism] argset_2", () => {
  const __args = [[0], ""];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_2", () => {
  const __args = [[0], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_2", () => {
  const __args = [[0], ""];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][8];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("unique [determinism] argset_3", () => {
  const __args = [[], "  spaced  "];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_3", () => {
  const __args = [[], "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_3", () => {
  const __args = [[], "  spaced  "];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][11];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("unique [determinism] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_4", () => {
  const __args = [[3, 1, 2], "Alpha42"];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][14];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("unique [determinism] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const first = __attempt("unique", __args);
  const second = __attempt("unique", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("unique [immutability] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const before = JSON.stringify(__args);
  const result = __attempt("unique", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("unique [snapshot] argset_5", () => {
  const __args = [[3, 1, 2], ""];
  const actual = JSON.stringify(__attempt("unique", __args));
  const stored = __snapshots["unique"] && __snapshots["unique"][17];
  if (__capture || stored === undefined) {
    __snapshots["unique"] = __snapshots["unique"] || {};
    __snapshots["unique"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("unique [edge_safety] edge_0", () => {
  const __args = [[], ""];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_1", () => {
  const __args = [[null], ""];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_2", () => {
  const __args = [[3, 1, 2], ""];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_3", () => {
  const __args = [[3, 1, 2], " "];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_4", () => {
  const __args = [[3, 1, 2], "\t"];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_5", () => {
  const __args = [[3, 1, 2], "0"];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("unique [edge_safety] edge_6", () => {
  const __args = [[3, 1, 2], "null"];
  try {
    __call("unique", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [determinism] argset_0", () => {
  const __args = ["alpha", "alpha"];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_0", () => {
  const __args = ["alpha", "alpha"];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_0", () => {
  const __args = ["alpha", "alpha"];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][2];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][2] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 2");
  }
});

test("deep_merge [determinism] argset_1", () => {
  const __args = ["hello world", "hello world"];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_1", () => {
  const __args = ["hello world", "hello world"];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_1", () => {
  const __args = ["hello world", "hello world"];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][5];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][5] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 5");
  }
});

test("deep_merge [determinism] argset_2", () => {
  const __args = ["", ""];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_2", () => {
  const __args = ["", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_2", () => {
  const __args = ["", ""];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][8];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][8] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 8");
  }
});

test("deep_merge [determinism] argset_3", () => {
  const __args = ["  spaced  ", "  spaced  "];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_3", () => {
  const __args = ["  spaced  ", "  spaced  "];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_3", () => {
  const __args = ["  spaced  ", "  spaced  "];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][11];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][11] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 11");
  }
});

test("deep_merge [determinism] argset_4", () => {
  const __args = ["Alpha42", "Alpha42"];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_4", () => {
  const __args = ["Alpha42", "Alpha42"];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_4", () => {
  const __args = ["Alpha42", "Alpha42"];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][14];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][14] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 14");
  }
});

test("deep_merge [determinism] argset_5", () => {
  const __args = ["", ""];
  const first = __attempt("deep_merge", __args);
  const second = __attempt("deep_merge", __args);
  assert.deepStrictEqual(second, first, "nondeterministic behaviour");
});

test("deep_merge [immutability] argset_5", () => {
  const __args = ["", ""];
  const before = JSON.stringify(__args);
  const result = __attempt("deep_merge", __args);
  if (result.threw === false) {
    const after = JSON.stringify(__args);
    assert.strictEqual(after, before, "input arguments were mutated");
  }
});

test("deep_merge [snapshot] argset_5", () => {
  const __args = ["", ""];
  const actual = JSON.stringify(__attempt("deep_merge", __args));
  const stored = __snapshots["deep_merge"] && __snapshots["deep_merge"][17];
  if (__capture || stored === undefined) {
    __snapshots["deep_merge"] = __snapshots["deep_merge"] || {};
    __snapshots["deep_merge"][17] = actual;
    __dirty = true;
  } else {
    assert.strictEqual(actual, stored, "snapshot mismatch for case 17");
  }
});

test("deep_merge [edge_safety] edge_0", () => {
  const __args = ["", ""];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_1", () => {
  const __args = [" ", ""];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_2", () => {
  const __args = ["\t", ""];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_3", () => {
  const __args = ["0", ""];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_4", () => {
  const __args = ["null", ""];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_5", () => {
  const __args = ["", " "];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_6", () => {
  const __args = ["", "\t"];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_7", () => {
  const __args = ["", "0"];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

test("deep_merge [edge_safety] edge_8", () => {
  const __args = ["", "null"];
  try {
    __call("deep_merge", __args);
  } catch (error) {
    assert.ok(error instanceof Error, "threw non Error value: " + String(error));
  }
});

